# mse-tracker

МХБ-ийн хувьцааны багцыг хянадаг систем. GitHub Actions өдөр бүр ажиллаж өгөгдөл
татаж, GitHub Pages дээр хяналтын самбар гаргана.

## Хэзээ ч зөрчиж болохгүй дүрмүүд

1. **`config_real.json`, `trades_real.csv`, `dividends_real.csv` гурвыг хэзээ ч
   коммит хийхгүй.** Эдгээрт бодит дүн байгаа, репо нь нээлттэй. `.gitignore`-д
   орсон байгаа, тэр мөрүүдийг хэзээ ч устгахгүй.
2. **Масштабын коэффициентийг файлд, коммитын тайлбарт, кодын тайлбарт, хаана ч
   бичихгүй.** Хэрэглэгч түүнийг зөвхөн толгойдоо хадгалдаг. Хэрэв коэффициент
   хэрэгтэй бол хэрэглэгчээс асуу, дараа нь хаана ч тэмдэглэхгүй.
3. **Бодит файл өөрчлөгдсөн бол `python scale_config.py <коэффициент>` заавал
   ажиллуулна.** Эс тэгвэл репод байгаа масштаблагдсан хувилбар хуучин үлдэнэ.
4. **`data/`, `docs/` доторх файлыг гараар засахгүй.** Тэдгээрийг `update.py`
   үүсгэдэг. Өөрчлөх шаардлагатай бол `update.py`-г засаад дахин ажиллуул.

## Файлын бүтэц

| Файл | Үүрэг |
|---|---|
| `config_real.json` | Зорилтот жин, сарын дүн, гараар оруулах үнэ (бодит) |
| `trades_real.csv` | Арилжааны бүртгэл: `date,symbol,shares,price,fee` |
| `dividends_real.csv` | Ногдол ашиг: `date,symbol,amount` (гарт орсон цэвэр дүн) |
| `scale_config.py` | Бодит гурван файлыг масштаблаж репод очих хувилбарыг үүсгэнэ |
| `update.py` | Өгөгдөл татах, тооцоолох, `docs/index.html` зурах |
| `performance.py` | Эзэмшил, XIRR, жишигтэй харьцуулалт |
| `mse_scraper.py` | open.mse.mn-ээс уншина |
| `.github/workflows/daily.yml` | Өдөр бүрийн автомат ажиллагаа |

Зарсан бол `trades_real.csv` дотор `shares` сөрөг тоогоор бичигдэнэ.

## Түгээмэл ажлууд

**Шинэ арилжаа нэмэх:** `trades_real.csv`-д мөр нэмэх → `scale_config.py`
ажиллуулах → `python update.py --no-fetch` хийж шалгах → коммит, push.

**Ногдол ашиг орсон:** `dividends_real.csv`-д мөр нэмэх → дээрхтэй ижил.

**Ногдол ашгийн шинэ шийдвэр зарлагдсан:** `config_real.json` доторх
`dividend_per_share` тоог шинэчлэх → дээрхтэй ижил.

**Хуудсыг өөрчлөх:** `update.py` доторх `render()` болон `CSS`. Өөрчилсний дараа
`python update.py --no-fetch` ажиллуулж үр дүнг шалга.

## Тест

Сүлжээнд хандахгүйгээр шалгах: `python update.py --no-fetch`. Энэ нь хуримтлагдсан
`data/prices.csv`-ээр хуудсыг дахин зурна. Ямар нэг өөрчлөлт хийсний дараа үүнийг
заавал ажиллуулж, алдаагүй дуусч байгааг батал.

## Кодын хэв маяг

- Тайлбар, хувьсагчийн нэр, хэрэглэгчид харагдах бүх текст монголоор
- Шинэ сан нэмэхээс зайлсхий. Одоогийн хамаарал: requests, beautifulsoup4, pandas.
  Нэмэх бол `daily.yml` доторх `pip install` мөрийг мөн шинэчил
- Файл жижиг, уншихад ойлгомжтой байлга. Хийсвэрлэл нэмэхээс илүү давталтыг зөвшөөр

## Энэ төсөлд санаатайгаар оруулаагүй зүйлс

Эдгээрийг санал болгох, нэмэх шаардлагагүй. Шалтгааныг нь урьд нь шинжилж үзсэн:

- Техник шинжилгээний үзүүлэлт (RSI, MACD), арилжааны дохио. Backtest хийхэд
  "хямдрахыг хүлээх" стратеги сар бүр тогтмол авахаас хоцорсон
- Бодит цагийн ханш. Нээлттэй эх сурвалжид байхгүй
- Өдөр тутмын мэдэгдэл. Сард нэг удаа шийдвэр гаргадаг систем

## Өгөгдлийн онцлог

- `open.mse.mn` албан ёсны API биш. Бүтэц өөрчлөгдвөл `mse_scraper.py` эвдэрнэ
- Арилжааны шимтгэл 1%. Энэ нь өндөр тул давтамжтай арилжаа хийх нь алдагдалтай
- Хөрвөх чадвар султай. Хязгаарын захиалга (limit order) үргэлж ашиглана
- ERS, MFG хоёрын үнэ автоматаар татагдахгүй, `manual_prices`-д гараар бичигдэнэ
